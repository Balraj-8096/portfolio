import { HightlightFiltervaluePipe } from './hightlight-filtervalue.pipe';

describe('HightlightFiltervaluePipe', () => {
  it('create an instance', () => {
    const pipe = new HightlightFiltervaluePipe();
    expect(pipe).toBeTruthy();
  });
});
